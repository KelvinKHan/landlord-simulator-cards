# 原始导出

本地基准文件：

- `房东模拟器Z5.20 (1).json`
- `房东模拟器Z5.20.png`

两份文件按原始字节复制，校验值记录在 `../docs/baseline.json`。
JSON 中的数据与 PNG 中 `chara`、`ccv3` 数据完全一致。

这两份文件已按用户要求纳入首次公开提交，克隆本仓库即可获得。
本目录仅保存基准备份；后续修改不要覆盖这里的文件。
