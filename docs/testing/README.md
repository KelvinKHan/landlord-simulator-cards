# 测试与验收

本轮 `npm run check`：15 项逻辑/导出测试、18 项 Chromium 联调全部通过，另通过原卡基准校验。真实酒馆集成流程也已通过。

自动检查使用 Node.js 24、Python 3 和 Chromium。开发依赖固定在 `package-lock.json`：Babel parser/traverse、esbuild、Playwright、fake-indexeddb、jQuery、lodash、Zod。没有新增玩家需要自行安装的脚本依赖。

```sh
npm ci
npx playwright install chromium
npm run check
npm run test:integration
```

`npm run check` 包含构建、逻辑/导出测试、真实 Chromium 中的隔离酒馆接口联调以及原始 JSON/PNG 的 SHA-256、CRC 校验。

自动覆盖：依赖慢启动和取消、队列失败恢复、聊天切换、连续版本切换、监听不累积、两个手机会话同时同步、不同模块更新同一本世界书、撤回空会话、消息与摘要事务、响应体取消、失败重试不重叠、分析批次失败不提前推进进度、候选人选择/档案编辑在原生重绘后保留、正式版筛选、更新文件损坏、缓存回退、世界书冲突保留与未完成事务恢复、JSON/PNG 一致和未知字段保留。

`npm run test:integration` 在 `.local/sillytavern-test/` 使用固定提交的真实 SillyTavern 和酒馆助手，每次创建独立测试数据并自动关闭测试服务。它从本地产物模拟项目 CDN，MVU/Zod 依赖使用固定提交的真实 CDN 构建；不接触个人酒馆配置。首次运行会下载隔离依赖。

[真实酒馆检查结果](real-sillytavern.json)记录版本与执行结果：一个角色脚本、原版/二改版启动、游戏内模式按钮、真实开场右划、MVU 变量改变、工坊更换开场、内嵌正则与状态占位符参与的档案重绘，以及无运行错误、无付费模型调用。

## 作者手动验收

1. 导入候选卡，确认酒馆助手角色脚本只有“房东模拟器 · 单入口”。
2. 用原版打开公寓和手机，再切二改版，确认界面切换且没有多份浮窗。
3. 在测试聊天中生成卧室、招募候选并选择一个人，编辑档案后等待一次页面重绘，确认选择与编辑仍在。
4. 保存档案、办理入住，检查卧室与住户显示；用手机发送消息后查看 ChatLore 摘要，撤回至空后确认摘要移除。
5. 发起副 API 请求时切换到另一段测试聊天，确认旧结果不会写进新聊天。
6. 使用工坊替换测试开场，确认文字与 MVU 状态一起更新。两套手机数据库沿用原设计，不据此验收跨版本历史迁移。

真实模型内容、主模型生成完整流程、EJS 插件参与的提示词构造，以及用户现有插件组合没有使用真实配额测试。本轮不会对这些未测范围作通过承诺。

## 分发检查

[GitHub Actions 记录](github-actions.json)中的主分支、版本标签与发布草稿工作流全部通过；Linux 构建产物与已提交文件一致。[公开 CDN 检查](cdn.json)确认入口、运行包、世界书内容包、清单及 JSON/PNG 六份文件均返回 HTTP 200、允许跨域，并与本地已验收文件逐字节一致。新版 PNG 也单独通过 CRC 及内嵌 JSON 校验。

候选版标签已上传，Release 保持草稿/预发布，尚未作为正式版推送给玩家。测试服务已关闭；隔离环境和记录保留在忽略提交的 `.local/`。
